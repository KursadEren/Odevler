#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;
int main()
{
    float kesır1, kesır2 , kesırT , a ,b ,c,d;
    cout << "a degerını gırınız = " << endl;
    cin >> a ;
    cout << " b degerını gırınız =" << endl;
    cin >> b;
    kesır1 = a/b ;
    cout << " c degerını gırınız = " << endl;
    cin >> c ;
    cout << " d degerını gırınız =" << endl;
    cin >> d;
    kesır2 = c/d;
    kesırT = (a*b + b*c) / b*d ;
    cout << kesırT 
}

// bir sıkıntı var ???? ; değil onu ben sildim işlem yanlış 
